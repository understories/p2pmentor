# p2pmentor Environment Variables
#
# Copy this file to .env and fill in the values
# .env is gitignored and will not be committed
#
# For Vercel deployment, set these in Vercel dashboard:
# Settings → Environment Variables

# =============================================================================
# Arkiv Configuration
# =============================================================================

# Private key for server-side entity creation (signing wallet)
# Format: 0x followed by 64 hex characters
# Required for: Server-side API routes that create entities
# Optional for: Client-only operations
# Example: ARKIV_PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
ARKIV_PRIVATE_KEY=

# Space ID for data isolation across environments
# Defaults: 'beta-launch' (production), 'local-dev' (development)
# Use different values to separate dev/staging/prod data
# Example: BETA_SPACE_ID=beta-launch
BETA_SPACE_ID=

# =============================================================================
# WalletConnect Configuration
# =============================================================================

# WalletConnect Cloud Project ID
# Required for: WalletConnect integration (Phase 0)
# How to get:
#   1. Go to https://cloud.walletconnect.com/
#   2. Sign up or log in
#   3. Create a new project (or use existing)
#   4. Copy the Project ID from the project dashboard
#   5. The Project ID is public and safe to expose in client builds
# Example: NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=abc123def456...
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Enable WalletConnect button on auth page
# Set to 'true' to show WalletConnect connection option
# Default: disabled (button hidden)
# Example: NEXT_PUBLIC_WALLETCONNECT_ENABLED=true
NEXT_PUBLIC_WALLETCONNECT_ENABLED=false

# =============================================================================
# Beta Access
# =============================================================================

# Beta invite code (public, client-side)
# Used for beta access control
# Set in Vercel environment variables for production
# Example: NEXT_PUBLIC_BETA_INVITE_CODE=your-invite-code
NEXT_PUBLIC_BETA_INVITE_CODE=

# =============================================================================
# Jitsi Video Configuration
# =============================================================================

# Jitsi instance base URL
# Defaults to: https://meet.jit.si
# Optional: Use custom Jitsi instance
# Example: JITSI_BASE_URL=https://meet.jit.si
JITSI_BASE_URL=

# =============================================================================
# Graph/Subgraph Configuration (Optional)
# =============================================================================

# Graph subgraph URL (optional, for future use)
# Example: GRAPH_SUBGRAPH_URL=https://api.thegraph.com/subgraphs/name/...
GRAPH_SUBGRAPH_URL=

# Enable subgraph for network queries (optional)
# Set to 'true' to use subgraph instead of direct Arkiv queries
# Example: USE_SUBGRAPH_FOR_NETWORK=false
USE_SUBGRAPH_FOR_NETWORK=

# =============================================================================
# Admin Configuration
# =============================================================================

# Admin password (server-side only, NOT NEXT_PUBLIC_)
# Required for: Admin routes (/admin)
# Set in Vercel environment variables (never commit)
# Example: ADMIN_PASSWORD=your-secure-password
ADMIN_PASSWORD=

# =============================================================================
# Node Environment
# =============================================================================

# Node environment (auto-set by Vercel in production)
# Development: NODE_ENV=development
# Production: NODE_ENV=production (auto-set by Vercel)
# Example: NODE_ENV=development
NODE_ENV=development

# =============================================================================
# Arkiv Review Mode
# =============================================================================

# Review mode password hash (public, build-time)
# SHA256 hash of the review mode password (24-32 random characters recommended)
# This is a PUBLIC hash - security relies on password entropy, not hash secrecy
# 
# To generate the hash:
#   printf "%s" "your_password_here" | shasum -a 256 | awk '{print $1}'
# OR
#   echo -n "your_password_here" | shasum -a 256 | cut -d ' ' -f 1
# 
# Example (for testing only - use a strong password in production):
#   printf "%s" "test_review_password_123" | shasum -a 256
#   Result: 42af718de4a209192e24300704d1d11839ede8c6bd2f210fb94743fa83c83496
#
# Requirements:
#   - Hash must be exactly 64 hex characters (lowercase)
#   - No spaces, no dashes, no newlines
#   - Set in Vercel environment variables for production
# Example: NEXT_PUBLIC_ARKIV_REVIEW_PASSWORD_SHA256=42af718de4a209192e24300704d1d11839ede8c6bd2f210fb94743fa83c83496
NEXT_PUBLIC_ARKIV_REVIEW_PASSWORD_SHA256=
