# Arkiv Configuration
# Private key for server-side entity creation (optional for client-only operations)
# Format: 0x... (64 hex characters)
ARKIV_PRIVATE_KEY=

# Jitsi Configuration
# Base URL for Jitsi Meet instance (defaults to https://meet.jit.si)
JITSI_BASE_URL=https://meet.jit.si

# Next.js Configuration
# Add any Next.js public environment variables here with NEXT_PUBLIC_ prefix
# Example: NEXT_PUBLIC_APP_NAME=p2pmentor

# Beta Access Configuration
# Invite code for beta access (set in Vercel, not in code)
# This should be shared word-of-mouth only, not in public documentation
NEXT_PUBLIC_BETA_INVITE_CODE=


# Admin Configuration
# Admin password for admin dashboard access (server-side only, not exposed to client)
# This should be set in Vercel environment variables, not in code
ADMIN_PASSWORD=

# Passkey (WebAuthn) Configuration
# Enable passkey login feature (beta)
# Set to 'true' to enable passkey authentication option on /auth page
ENABLE_PASSKEY_LOGIN=false
NEXT_PUBLIC_ENABLE_PASSKEY_LOGIN=false

# WebAuthn Relying Party configuration (optional, has defaults)
# PASSKEY_RP_ID: REQUIRED for production - must match your domain (e.g., "www.p2pmentor.com")
#   Defaults to hostname, but should be explicitly set in production for security
# PASSKEY_RP_NAME: Optional - display name in browser dialogs (defaults to "p2pmentor")
# PASSKEY_ORIGIN: Usually not needed - defaults to window.location.origin
#   Only set if behind a proxy or origin doesn't match
# PASSKEY_RP_ID=
# PASSKEY_RP_NAME=p2pmentor
# PASSKEY_ORIGIN=
